# This file should be committed to your app code.
version: 1.0
name: whats_app

requirements:
  skip: false
  conda:
    channels:
      - conda-forge
    packages:
      - twilio
      - shortuuid
      - requests
  pip:
post:
